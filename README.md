# Laravel 8 Boilerplate project

## Setup

-   Clone repository
-   Create configuration file `cp .env.example .env`
-   Generate app key `php artisan key:generate`
-   Update dependencies `composer update`
